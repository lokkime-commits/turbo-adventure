🎯 Задание 1 (Урок 1 — print)
Выведи три строки:

text
Python — это сила!
Я учусь программировать
Экзамен начался!

print("Python — это сила!\nЯ учусь программировать\nЭкзамен начался!")

🎯 Задание 2 (Урок 2 — переменные)
Создай переменные:
name с твоим именем
age с твоим возрастом
city с твоим городом
Выведи фразу:
"Меня зовут X, мне Y лет, я из Z"

name = "Ilia"
age = 37
city = "Zelenogradsk"
print(f"Меня зовут {name}, мне {age} лет, я из {city}")

🎯 Задание 3 (Урок 3 — типы данных)
Дан код:
python
a = 10
b = 3.14
c = "Hello"
d = True
Напиши код, который выведет тип каждой переменной (используй type()).

print(type(a))
print(type(b))
print(type(c))
print(type(d))

🎯 Задание 4 (Урок 4 — input)
Спроси у пользователя любимый цвет и выведи:
"Твой любимый цвет — [цвет]"

любимый_цвет = input("Какой твой любимый цвет?")
print(f"Твой любимый цвет - {любимый_цвет}")

🎯 Задание 5 (Урок 5 — if-elif-else)
Спроси у пользователя число от 1 до 7 и выведи день недели:

Число	День
1	    Понедельник
2	    Вторник
3	    Среда
4	    Четверг
5	    Пятница
6	    Суббота
7	    Воскресенье
Если число вне диапазона — вывести "Ошибка! Введи число от 1 до 7".

число = int(input("Назови число от 1 до 7: "))
if число == 1:
    print("Понедельник")
elif число == 2:
    print("Вторник")
elif число == 3:
    print("Среда")
elif число == 4:
    print("Четверг")
elif число == 5:
    print("Пятница")
elif число == 6:
    print("Суббота")
elif число == 7:
    print("Воскресенье")
else:
    print("Ошибка! Введи число от 1 до 7")

🎯 Задание 6 (Урок 6 — списки)
Создай список из 5 фруктов: "яблоко", "банан", "апельсин", "манго", "киви"
Выведи первый фрукт
Выведи третий фрукт
Выведи последний фрукт

fruits = ["яблоко", "банан", "апельсин", "манго", "киви"]
print(fruits[0])
print(fruits[2])
print(fruits[-1])

# Создай список из 5 любых чисел
# Выведи на экран:
# 1. Первые три элемента (срез [0:3])
# 2. Последние два элемента (срез [3:5])
# 3. Весь список целиком

numbers = [2, 4, 5, 7, 8]
print(numbers[0:3])
print(numbers[3:5])
print(numbers)

🎯 Задание 7 (Урок 7 — методы списков)
Возьми список из задания 6 и выполни по порядку:
Добавь в конец списка "виноград"
Вставь "груша" на позицию 1 (второй элемент)
Удали "банан" из списка
Отсортируй список
Выведи итоговый список

fruits = ["яблоко", "банан", "апельсин", "манго", "киви"]
fruits.append('виноград')
fruits.insert(1, "груша")
fruits.remove("банан")
fruits.sort()
print(fruits)

🎯 Задание 8 (Урок 8 — цикл for)
Выведи числа от 1 до 10 и их квадраты в формате:
text
1 → 1
2 → 4
3 → 9
...
10 → 100

for number in range(1, 11):
    square = number ** 2
    print(f"{number} → {square}")

# Дан список: ["а", "б", "в", "г", "д"]
# Используй цикл for и range(len()) чтобы вывести каждый элемент с индексом
# Формат вывода: "Индекс 0: а"
my_list = ["а", "б", "в", "г", "д"]
for i in range(len(my_list)):
    print(f"{i}: {my_list[i]}")

# Дан список чисел: [4, 8, 15, 16, 23, 42]
# Используй цикл for, чтобы найти и вывести:
# 1. Сумму всех элементов
# 2. Количество элементов (используй len())
# 3. Среднее арифметическое (сумма / количество)

numbers = [4, 8, 15, 16, 23, 42]
total = 0
for x in numbers:
    total = total + x
count = len(numbers)
average = total / count

print(total)
print(count)
print(average)

🧠 Трудность с len() и average — это нормально!
Ты запомнил эти строки, и теперь они твои. Вот как их запомнить ещё крепче:

Код	Что значит	Как запомнить
len(numbers)	длина списка (количество элементов)	Length → длина
total / count	сумма делённая на количество	Это определение среднего

🎯 Задание 9 (Урок 9 — цикл while)
Программа спрашивает пароль.
Пароль правильный: "secret123".
Цикл работает, пока пользователь не введёт правильный пароль.
При каждой ошибке выводи: "Неверно, попробуй ещё раз!"

true_password = "secret123"
password_entered = ""

while password_entered != true_password:
    password_entered = input("Введите пароль: ")
    if password_entered != true_password:
        print("Неверно, попробуй ещё раз!")

print("Доступ разрешён")


# Напиши программу, которая:
# 1. Создаёт переменную count = 5
# 2. Использует цикл while, чтобы вывести числа от 5 до 1 (обратный отсчёт)
# Подсказка: условие будет while count >= 1, а счётчик нужно уменьшать (count -= 1).

count = 5
while count >= 1:
    print(count)
    count -= 1

# Напиши программу, которая:
# 1. Спрашивает у пользователя "Введите пароль: "
# 2. Если пароль правильный ("12345"), выводит "Доступ разрешён" и выходит из цикла
# 3. Если пароль неправильный, выводит "Неверный пароль, попробуйте снова" и спрашивает снова
# 4. Используй цикл while

password = int(input("Введите пароль: "))   # Спросили один раз

while password != 12345:                   # Пока пароль неправильный
    print("Неверный пароль, попробуйте снова")
    password = int(input("Введите пароль: "))   # Спрашиваем заново

print("Доступ разрешён")

# Используй цикл while, чтобы вывести все чётные числа от 2 до 10 включительно
# Подсказка: используй счётчик, проверку на чётность % 2 == 0 и увеличение счётчика.
    
count = 2
while count <= 10:
    print(count)
    count += 2 

# Используй цикл while, чтобы найти сумму всех чисел от 1 до 10
# Выведи результат: "Сумма чисел от 1 до 10 равна 55"
# Подсказка: создай total = 0 и на каждом шаге добавляй count к total.
    
total = 0
count = 1

while count <= 10:
    total = total + count
    count = count + 1

print(f"Сумма чисел от 1 до 10 равна {total}")

# Используй цикл while, чтобы вывести все нечетные числа от 1 до 20.
# Вывод должен быть в одной строке через пробел.
# Подсказка: используй проверку % 2 != 0 и накапливай строку.


number = 1
while number <= 20:
    if number % 2 != 0:
        print(number)
    number = number + 1
    
number = 1
result = ""                      # пустая строка для результата
while number <= 20:
    if number % 2 != 0:
        result = result + str(number) + " "   # добавляем число и пробел
    number = number + 1

print(result.strip()) 

# Используй цикл while, чтобы вывести таблицу умножения на 3:
# 3 × 1 = 3
# 3 × 2 = 6
# ...
# 3 × 10 = 30
1	# Создаём счётчик multiplier = 1
2	# Запускаем цикл while multiplier <= 10:
3	# Внутри цикла выводим 3 × multiplier = 3 * multiplier
4	# Увеличиваем multiplier на 1
5	# Когда multiplier станет 11 — цикл завершается

multiplier = 1
while multiplier <= 10:
    print(f"3 × {multiplier} = {3 * multiplier}")
    multiplier = multiplier + 1
    if multiplier == 11:
        break
# Напиши программу "Угадай число":
# 1. Загадай число (например, secret = 7)
# 2. В цикле while спрашивай у пользователя число
# 3. Если число меньше загаданного — выводи "Больше!"
# 4. Если число больше загаданного — выводи "Меньше!"
# 5. Если число равно загаданному — выводи "Поздравляю! Ты угадал!" и завершай цикл

secret = 7
number = 0

while number != secret:
    number = int(input("Угадай число: "))
    if number > secret:
        print("выводи Меньше!")
    elif number < secret:
        print("выводи Больше!")
    else:
        print("Поздравляю! Ты угадал!")

# Напиши программу "Угадай число":
# 1. Загадай число (например, secret = 7)
# 2. В цикле while спрашивай у пользователя число
# 3. Если число меньше загаданного — выводи "Больше!"
# 4. Если число больше загаданного — выводи "Меньше!"
# 5. Если число равно загаданному — выводи "Поздравляю! Ты угадал!" и завершай цикл

secret = 7

while True:
    number = int(input("Угадай число: "))
    if number > secret:
        print("Меньше!")
    elif number < secret:
        print("Больше!")
    else:
        print("Поздравляю! Ты угадал!")
        break

# Напиши программу, которая считает количество цифр в числе.
# Пользователь вводит число, а программа выводит, сколько в нём цифр.
# Используй цикл while.
# Например: 12345 → 5 цифр, 7 → 1 цифра.
# Подсказка: нужно делить число на 10, пока оно не станет 0. Считай шаги.

number = int(input("Введи число: "))
count = 0

while number != 0:
    number = number // 10   # отбрасываем последнюю цифру
    count = count + 1       # считаем шаг

print(f"Количество цифр в числе: {count}")

# Напиши программу, которая запрашивает у пользователя числа
# и выводит только те, которые делятся на 3 без остатка.
# Программа завершается, когда пользователь вводит 0.
# Используй цикл while.

count = 0
while True:
    number = int(input("Введите число (0 для выхода): "))
    if number == 0:
        break                # выходим из цикла
    if number % 3 == 0:      # проверяем, делится ли на 3
        print(number)        # выводим число
        count = count + 1    # считаем такие числа

print(f"Количество чисел, делящихся на 3: {count}")


__________________________
Важное правило (запомни!)

total = 0
count = 1

while count <= 10:
    total = total + count   # добавляем текущее число
    count = count + 1       # переходим к следующему
Эту конструкцию ты будешь использовать очень часто.

🎯 Запомни формулу
python
if number % 2 != 0:    # число НЕЧЁТНОЕ
    print(number)
% — даёт остаток от деления.
Если остаток не равен 0 — число нечётное.

___________________________

🎯 Задание 10 (Урок 10 — словари)
Создай словарь person с ключами: "name", "age", "city"
Заполни его любыми данными
Выведи все ключи и значения через цикл for key, value in person.items():

dictionary = {
    "name": "Ilia",
    "age": 37,
    "city": "Zelenogradsk"
}
for key, value in dictionary.items():
    print(f"{key}: {value}")

🔧 Основные операции со словарями
Действие	Код
Создать словарь	person = {"имя": "Илья"}
Получить значение по ключу	person["имя"] → "Илья"
Добавить новую пару	person["работа"] = "программист"
Изменить значение	person["возраст"] = 38
Удалить ключ	del person["город"]
Перебрать все ключи и значения	for key, value in person.items():

# Используя цикл for, выведи все ключи и значения словаря person
# Формат вывода: "ключ: значение" (каждый на новой строке)
# Подсказка: используй .items():

person = {
    "name": "Илья",
    "age": 37,
    "city": "Зеленоградск"
}
person["job"] = ["программист"]
person["age"] = 38
for key, value in person.items():
    print(f"{key}: {value}")

🎯 Задание 11 (Урок 11 — функции def)
Напиши функцию square, которая принимает одно число и возвращает его квадрат.
Проверь: print(square(5)) → должно вывести 25

def square(x):
    return x ** 2

print(square(8))

🎯 Задание 12 (Урок 12 — параметры функций)
Напиши функцию greet, которая принимает имя и выводит фразу:
"Привет, [имя]!"

Пример: greet("Илья") → Привет, Илья!

def greet(name):
    print(f"Привет, {name}!")

greet("Илья")

# Напиши функцию add(a, b), которая возвращает сумму a и b
# Вызови её с числами 5 и 3, результат напечатай
# Подсказка: функция должна использовать return, а не print.

def add(a, b):
    sum = a + b
    return sum

result = add(5, 3)   # сохраняем результат в переменную
print(result) 
Или короче:
def add(a, b):
    return a + b

print(add(5, 3)) 
🎯 Задание 13 (Урок 13 — return)
Напиши функцию is_even, которая принимает число и возвращает True, если чётное, и False — если нечётное.

Пример: print(is_even(4)) → True, print(is_even(7)) → False

def is_even(number):
    if number % 2 == 0:
        print("чётное")
    else:
        print("нечётное")

is_even(4)
is_even(7)

или (этот праильнее)

def is_even(number):
    return number% 2 == 0
print(is_even(4)) 
print(is_even(7))

# Напиши функцию is_even(number), которая возвращает True, если число чётное, и False, если нечётное
# Вызови её с числами 4 и 7, результат напечатай
# Подсказка: используй оператор % для проверки чётности.

def is_even(number):
    if number % 2 == 0:
        return True
    else:
        return False

print(is_even(4))   # должно быть True
print(is_even(7))

🎯 Задание 14 (Урок 14 — область видимости)
Создай глобальную переменную counter = 0
Напиши функцию increment, которая увеличивает counter на 1 (используй global)
Вызови функцию 3 раза
Напечатай counter

counter = 0
def increment():
    global counter
    counter += 1
increment()
increment()
increment()
print(counter)

🎯 Задание 15 (Урок 15 — модули)
Импортируй модуль random и выведи случайное число от 1 до 100.

import random  
random_number = random.randint (1, 100)
print(f"A random number between 1 and 100: {random_number}")

# Импортируй модуль random
# Создай список из трёх имён: ["Анна", "Илья", "Мария"]
# Выбери случайное имя из списка и выведи его на экран
Подсказка: используй random.choice(список).

import random
random_list = random.choice(["Анна", "Илья", "Мария"])
print(f"{random_list}")

# Импортируй модуль random
# Создай список чисел: [10, 20, 30, 40, 50]
# Перемешай список случайным образом (используй random.shuffle())
# Выведи перемешанный список на экран
# Подсказка: random.shuffle(список) изменяет список на месте.

import random

numbers = [10, 20, 30, 40, 50]
random.shuffle(numbers)
print(numbers)

# Импортируй модуль random
# Напиши функцию generate_password(length), которая:
# - генерирует случайный пароль из букв и цифр
# - возвращает строку длиной length
# - используй random.choice() и строку "abcdefghijklmnopqrstuvwxyz0123456789"
# Вызови функцию с длиной 8 и напечатай результат

import random

def generate_password(length):
    chars = "abcdefghijklmnopqrstuvwxyz0123456789"
    password = ""
    for i in range(length):
        password = password + random.choice(chars)
    return password

result = generate_password(8)
print(result)

✅ Как правильно
Нужно:
Создать строку chars со всеми возможными символами
В цикле for длиной length брать случайный символ
Собирать их в строку и возвращать

🎯 Задание 16 (Урок 16 — чтение файлов)
Вручную создай файл test.txt с одной строкой: "Привет, мир!"

Напиши код, который открывает этот файл, читает и выводит содержимое

with open("test.txt", "r", encoding="utf-8") as file:
    content = file.read()
    print(content)

# Создай файл lines.txt с тремя строками текста (вручную)
# Напиши программу, которая читает файл построчно (используя readlines())
# и выводит каждую строку с её номером:
# 1: строка_1
# 2: строка_2
# 3: строка_3

with open("lines.txt", "r", encoding="utf-8") as file:
    lines = file.readlines()
    for line in lines:
        print(line.strip())  Функция `.strip()` удаляет начальные и конечные пробелы, включая символы новой строки.

# Создай файл data.txt с числами (каждое на новой строке):
# 10
# 20
# 30
# 40
# 50
# Напиши программу, которая:
# 1. Читает файл построчно
# 2. Преобразует каждую строку в число (int)
# 3. Находит и выводит сумму всех чисел

total = 0
with open("data.txt", "r", encoding="utf-8") as file:
    for line in file:
        number = int(line.strip())
        total = total + number
print(f"сумма всех чисел {total}")

# Напиши программу, которая:
# 1. Читает файл data.txt
# 2. Находит и выводит самое большое число в файле
# 3. Если файл пустой, выводит "Файл пуст"
# Подсказка: используй переменную max_number = None или max_number = 0.
max_number = 0
with open("data.txt", "r", encoding="utf-8") as file:
    for line in file:
        number = int(line.strip())
        if number > max_number:
            max_number = number
print(f"самое большое число в файле {max_number}")



🎯 Задание 17 (Урок 17 — запись в файлы)
Напиши код, который:
Открывает файл log.txt для добавления (режим "a")
Записывает в него строку: "Запуск программы"
Каждый запуск добавляет новую строку

with open("log.txt", "a", encoding="utf-8") as f:
    f.write("\nЗапуск программы\n")

💡 Почему \n в начале и конце
python
f.write("\nЗапуск программы\n")
\n в начале — отступ перед записью
\n в конце — перевод строки после записи
Это гарантирует, что каждая запись будет на новой строке.

🎯 Задание 18 (Урок 18 — try/except)
Напиши код, который:
Просит пользователя ввести число
Преобразует в int
Если ввели не число — ловит ValueError и выводит "Ошибка! Нужно ввести число"

try:
    number_str = input("Enter your number: ")
    number = int(number_str)
    print(f"You number {number}")
except ValueError:
    print("Ошибка! Нужно ввести число")
